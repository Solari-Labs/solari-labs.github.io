<footer class="footer">
  <div class="container">
    <div class="f-grid">
      <div class="f-brand">
        <img src="/logo.png" alt="" width="24" height="24" class="f-logo-img" />
        <span>Solari Labs</span>
        <p class="f-tag">Powering the next AI revolution</p>
      </div>
      <div class="f-links">
        <span class="f-h">Product</span>
        <a href="/orion">Orion</a>
        <a href="/zenith">Zenith</a>
        <a href="/benchmarks">Benchmarks</a>
      </div>
      <div class="f-links">
        <span class="f-h">Company</span>
        <a href="/contact">Contact</a>
        <a href="https://github.com/Solari-Labs" target="_blank">GitHub</a>
      </div>
    </div>
    <p class="f-copy">© {new Date().getFullYear()} Solari Labs</p>
  </div>
</footer>

<style is:global>
.footer {
  border-top: 1px solid var(--border);
  padding: 3rem 0 2rem;
  background: var(--bg-elevated);
}
.f-grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 3rem;
  margin-bottom: 2rem;
}
.f-brand {
  display: flex; flex-direction: column; gap: 6px;
  font-family: var(--font-heading); font-weight: 700;
}
.f-logo-img { border-radius: 50%; }
.f-tag { font-family: var(--font-body); font-weight: 400; font-size: 0.85rem; color: var(--text-muted); }
.f-h {
  font-size: 0.7rem; font-weight: 700; text-transform: uppercase;
  letter-spacing: 0.12em; color: var(--text-muted); margin-bottom: 0.75rem; display: block;
}
.f-links a {
  display: block; font-size: 0.85rem; color: var(--text); padding: 3px 0;
  transition: opacity 0.2s;
}
.f-links a:hover { opacity: 0.6; }
.f-copy { font-size: 0.8rem; color: var(--text-muted); border-top: 1px solid var(--border); padding-top: 1.5rem; }
@media (max-width: 600px) { .f-grid { grid-template-columns: 1fr; gap: 2rem; } }
</style>
