---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
---

<BaseLayout title="Home">
  <Hero />
  <section class="section" id="orion-preview">
    <div class="container">
      <span class="section-label">Flagship Product</span>
      <h2 class="section-title">
        Meet <span class="gradient-text">Orion</span>
      </h2>
      <p class="section-subtitle">
        An AI engine written in C from scratch. Designed for training and inference with
        unprecedented performance. 33x faster than traditional alternatives.
      </p>
      <div class="preview-cards">
        <a href="/orion" class="card preview-card">
          <div class="preview-card-icon">⚡</div>
          <h3>Extreme Performance</h3>
          <p>Hardware-level optimizations that maximize every compute cycle.</p>
          <span class="preview-card-link">Explore Orion →</span>
        </a>
        <a href="/zenith" class="card preview-card">
          <div class="preview-card-icon">✦</div>
          <h3>Zenith Models</h3>
          <p>Trained on a laptop to demonstrate Orion's true potential.</p>
          <span class="preview-card-link">View Models →</span>
        </a>
        <a href="/benchmarks" class="card preview-card">
          <div class="preview-card-icon">📊</div>
          <h3>Benchmarks</h3>
          <p>Measurable results backing every claim. Complete transparency.</p>
          <span class="preview-card-link">See Data →</span>
        </a>
      </div>
    </div>
  </section>
  <section class="section section-stats">
    <div class="container">
      <div class="stats-grid">
        <div class="stat-item">
          <span class="stat-number gradient-text">33×</span>
          <span class="stat-label">Faster</span>
        </div>
        <div class="stat-item">
          <span class="stat-number gradient-text">100%</span>
          <span class="stat-label">Open Source</span>
        </div>
        <div class="stat-item">
          <span class="stat-number gradient-text">C99</span>
          <span class="stat-label">Core Language</span>
        </div>
        <div class="stat-item">
          <span class="stat-number gradient-text">0</span>
          <span class="stat-label">Dependencies</span>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style is:global>
.preview-cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
  margin-top: 48px;
}
.preview-card {
  text-decoration: none;
  color: var(--text-primary);
  display: flex;
  flex-direction: column;
  gap: 16px;
}
.preview-card-icon {
  font-size: 2rem;
  line-height: 1;
}
.preview-card h3 {
  font-size: 1.25rem;
}
.preview-card p {
  color: var(--text-secondary);
  font-size: 0.9rem;
  line-height: 1.6;
  flex: 1;
}
.preview-card-link {
  color: var(--accent);
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 0.85rem;
}
.preview-card:hover .preview-card-link {
  text-decoration: underline;
}
.section-stats {
  background: var(--bg-elevated);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}
.stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 32px;
  text-align: center;
}
.stat-item {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.stat-number {
  font-family: var(--font-heading);
  font-size: clamp(2.5rem, 6vw, 4rem);
  font-weight: 800;
  letter-spacing: -0.04em;
}
.stat-label {
  color: var(--text-secondary);
  font-size: 0.9rem;
  font-weight: 500;
}
@media (max-width: 768px) {
  .preview-cards {
    grid-template-columns: 1fr;
  }
  .stats-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 40px;
  }
}
</style>
